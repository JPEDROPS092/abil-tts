# Core TTS
qwen-tts
edge-tts
torch==2.11.0
triton==3.6.0
piper-tts
onnxruntime-gpu
# Coqui TTS (Idiap-maintained fork; the original `TTS` package is unmaintained and
# only supports Python <=3.11). `coqui-tts` keeps the same `from TTS.api import TTS`
# import path. Requires Python 3.10–3.12.
coqui-tts
soundfile
numpy

# Cloud TTS
google-genai    # Gemini TTS (gemini-3.1-flash-tts-preview)

# LLM (OpenAI-compatible client — MaaS, OpenAI, etc.)
openai

# Document formats
python-docx
pymupdf       # PDF support (import as fitz)

# Web API
fastapi
uvicorn[standard]
python-multipart

# Optional: desktop audio playback (Linux)
# python-vlc
